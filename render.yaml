services:
  - type: web
    name: nexusfinance
    runtime: docker
    repo: https://github.com/ndxdigitalsupport/nexusfinance
    branch: main
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3001"
